# Foundry VTT Prototype Infrastructure Context

This document captures the current state of your running AWS infrastructure for the Foundry VTT prototype. We will use this exact context to architect the CDK stack and replace the manual setup with declarative code.

## 1. Prototype EC2 Instance (`i-0f637b84deaace1b9`)
- **Region**: `us-west-2`
- **Availability Zone**: `us-west-2b`
- **Instance Type**: `t3.medium`
- **AMI ID**: `ami-0a914de4dc1f18727` (Amazon Linux)
- **Key Pair**: `ftt`
- **Public IP**: `35.95.143.128`
- **IAM Instance Profile**: Attached to the role we generated earlier (`ReallyFaroutCoolStack-FoundryVttInstanceProfile...`), which grants it the exact S3 read/write permissions for the `really-farout.cool-foundry-data` bucket.

## 2. Networking footprint
- **VPC**: The instance is currently deployed in your account's **Default VPC** (`vpc-0ef65e69`), which has a CIDR block of `172.31.0.0/16`.
- **Subnet**: Specifically running inside `subnet-0350b04a`.
- **Security Group**: Attached to `launch-wizard-5` (`sg-0bb423f6774d15121`), which is acting as a classic web server group. It has Ingress open to `0.0.0.0/0` on:
  - Port `22` (SSH)
  - Port `80` (HTTP)
  - Port `443` (HTTPS)

## 3. Route53 DNS (`really.farout.cool`)
I queried Route53 for the hosted zones regarding your hostname. 
- You **do not** have a dedicated hosted zone specifically for `really.farout.cool`. 
- Instead, the apex domain **`farout.cool.` is the Hosted Zone** (ID: `Z04445774113CCTGXAT7`). 

> [!TIP]
> **CDK Implication:** When we build out the CDK logic for this, we will need to query Route53 for the `farout.cool` hosted zone, and then explicitly define an `A Record` for the `really` subdomain that points to the Public IP or Elastic IP of the new EC2 instance we synthesize!

---
*(Generated by Antigravity from active AWS account metadata)*
